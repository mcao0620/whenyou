// used currently in GroupFindMembers.tsx for when group admins
// search for potential users to add to their group.
export const STRING_SIMILARITY_CONST = 0.5;

// maximum group size currently supported.
export const MAX_GROUP_SIZE = 12;

// length of uniquely generated group ID
export const GROUP_ID_LENGTH = 6;

// length of longest first name, last name, username, groupname
export const MAX_NAME_LENGTH = 50;

// length of phone number
export const PHONE_NUMBER_LEN = 10;

// length of phone verification code sent from firebase
export const VERIFY_PHONE_LEN = 6;

// maximum group description size currently allowed.
export const MAX_DESCRIPTION_LENGTH = 75;

// group info screen width constants
// tailwind css for 11/12ths of screen
export const WIDTH_CONST = 'w-11/12';
